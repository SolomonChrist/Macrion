// STUB — replaced by the Game/Quest builder.
// createGame(ctx) -> { name:'game', update(ctx), state, quest, advance(id), on(evt,fn) }
export function createGame() {
  return { name: 'game', update() {}, state: {}, quest: null };
}
